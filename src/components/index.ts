/**
 * components/
 *
 * Future home of shared UI components, e.g.:
 *   - StatusBadge    — colored pill showing online/offline/error
 *   - PeerList       — scrollable list of connected peers
 *   - TaskCard       — displays a single compute task and its progress
 *   - MetricGauge    — animated gauge for compute / memory / network metrics
 *
 * Placeholder — not yet implemented.
 */
